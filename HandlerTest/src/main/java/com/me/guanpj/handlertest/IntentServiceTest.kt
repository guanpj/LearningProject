package com.me.guanpj.handlertest

import android.app.IntentService
import android.content.Intent

class IntentServiceTest(name: String?) : IntentService(name) {
    override fun onHandleIntent(intent: Intent?) {
    }
}